{
    executable: {{{ROOT_DIR}}}/bin/accuser
    args: [
        --remote-signer
        {{{model.REMOTE_SIGNER_ADDR}}}
        run
        {{#configuration.ACCUSER_STARTUP_ARGS}}
        {{.}}
        {{/configuration.ACCUSER_STARTUP_ARGS}}
    ]
    environment: {
        HOME: {{{ROOT_DIR}}}/data
        TEZOS_LOG: "* -> debug"
    }
    restart: always
    restart_delay: 20
}