{
    nodes: {
        main: {
            named_tunnels: {
                tezos-dal: {
                    disabled: ${dal_disabled}
                    exit_to: ${node_rpc_endpoint}
                    enter_from: ${dal_rpc_endpoint}
                }
            }
        }
    }
}
