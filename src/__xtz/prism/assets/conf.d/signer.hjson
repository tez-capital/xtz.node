{
    nodes: {
        main: {
            named_tunnels: {
                tezos-signer: {
                    disabled: ${signer_disabled}
                    exit_to: ${node_rpc_endpoint}
                    enter_from: ${signer_endpoint}
                }
            }
        }
    }
}
